# /plan-atomize

Você está na etapa 3 de 5 do refinamento de plano.

> Os arquivos `PLAN.md` e `HANDOFF.md` ficam na **raiz do projeto** (mesma pasta onde este projeto é aberto). Sempre leia e salve a partir de lá.

## Sua tarefa

1. Leia o `PLAN.md` atual (na raiz do projeto)
2. Reescreva cada etapa garantindo:
   - Exatamente um verbo de ação por passo (criar, chamar, verificar, instalar, etc.)
   - Um critério de conclusão mensurável (ex: "arquivo X existe", "rota responde 200")
   - Nenhuma inferência necessária — tudo explícito
3. Quebre etapas compostas em etapas menores se necessário
4. Salve o `PLAN.md` atualizado (na raiz do projeto)

## Ao finalizar

Atualize o `HANDOFF.md` (na raiz do projeto) com o seguinte conteúdo:

```
# Handoff

etapa_concluida: 3-atomize
proximo_comando: /plan-resilience
status: em_andamento

## Resumo da sessão
[descreva em 2-3 linhas o que foi alterado]
```

Salve ambos os arquivos e informe ao usuário que a sessão pode ser encerrada.
